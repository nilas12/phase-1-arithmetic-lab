function multiply(){
    let num1 = 31
    let num2 = 2
  
    return num1*num2 }
    function random(max) {
        return Math.floor(Math.random() * max);
      }
      console.log(Math.random());
      function max(numArray) {
        return Math.max.apply(null, numArray);
      }
      function mod(x) {
        return math.mod(x, 2) != 0
      }
      //I DO NOT KNOW WHY NOT PASSING THE TEST??